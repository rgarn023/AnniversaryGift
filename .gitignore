# Godot
.godot/
*.translation
export_credentials.cfg

# Build outputs (keep the debug APK committed for download)
build/*.aab
build/*.idsig
*.aab
*.idsig

# Android Gradle build template (reinstall with tools/install_android_template.sh)
android/build/
android/build/.gradle/
android/build/build/
android/build/local.properties
android/build/.cxx/
*.keystore
*.jks

# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/

# Secrets — never commit keystores or passwords
secrets/
*.pem
key.properties
